<%- include('../partials/head') %>
<%- include('../partials/nav') %>

<main class="container pb-5">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h4 mb-0">Khách hàng</h1>
    <div class="d-flex gap-2">
      <form method="get" action="/admin/customers" class="d-flex gap-2">
        <% if (showDeleted) { %><input type="hidden" name="deleted" value="1" /><% } %>
        <input class="form-control form-control-sm" name="q" value="<%= q || '' %>" placeholder="name/phone/email..." />
        <button class="btn btn-sm btn-outline-secondary" type="submit">Tìm</button>
      </form>
      <a class="btn btn-sm btn-outline-secondary" href="/admin/customers">Đang dùng</a>
      <a class="btn btn-sm btn-outline-secondary" href="/admin/customers?deleted=1">Đã xóa</a>
      <a class="btn btn-primary btn-sm" href="/admin/customers/new">Thêm mới</a>
    </div>
  </div>

  <% if (!rows || rows.length === 0) { %>
    <div class="alert alert-secondary">Trống.</div>
  <% } else { %>
    <div class="table-responsive">
      <table class="table align-middle">
        <thead>
          <tr>
            <th>#</th>
            <th>Tên</th>
            <th>SĐT</th>
            <th>Email</th>
            <th>Tài khoản</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% rows.forEach((c) => { %>
            <tr>
              <td><%= c.id %></td>
              <td><%= c.ten %></td>
              <td><%= c.sdt %></td>
              <td><%= c.email || '' %></td>
              <td><%= c.user ? c.user.email : '' %></td>
              <td class="text-end">
                <% if (!showDeleted) { %>
                  <a class="btn btn-sm btn-outline-secondary" href="/admin/customers/<%= c.id %>/edit">Sửa</a>
                  <form method="post" action="/admin/customers/<%= c.id %>" class="d-inline">
                    <input type="hidden" name="_method" value="DELETE" />
                    <button class="btn btn-sm btn-outline-danger" type="submit">Xóa</button>
                  </form>
                <% } else { %>
                  <form method="post" action="/admin/customers/<%= c.id %>/restore" class="d-inline">
                    <input type="hidden" name="_method" value="PATCH" />
                    <button class="btn btn-sm btn-outline-success" type="submit">Khôi phục</button>
                  </form>
                <% } %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% } %>

  <div class="d-flex justify-content-between align-items-center mt-3">
    <div class="text-muted small">Trang <%= page %></div>
    <div class="d-flex gap-2">
      <% if (page > 1) { %>
        <a class="btn btn-sm btn-outline-secondary" href="/admin/customers?<%= new URLSearchParams({ deleted: showDeleted ? '1' : '', q: q || '', page: String(page - 1), pageSize: String(pageSize) }).toString() %>">Trước</a>
      <% } %>
      <% if (hasNext) { %>
        <a class="btn btn-sm btn-outline-secondary" href="/admin/customers?<%= new URLSearchParams({ deleted: showDeleted ? '1' : '', q: q || '', page: String(page + 1), pageSize: String(pageSize) }).toString() %>">Sau</a>
      <% } %>
    </div>
  </div>
</main>

<%- include('../partials/foot') %>
