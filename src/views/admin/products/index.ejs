<%- include('../partials/head') %>
<%- include('../partials/nav') %>

<main class="container-fluid pb-5">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 fw-bold mb-0 text-dark">Sản phẩm</h1>
    <a class="btn btn-primary rounded-pill px-4 fw-medium shadow-sm transition hover-lift" href="/admin/products/new"><i class="bi bi-box-seam me-2"></i>Thêm sản phẩm</a>
  </div>

  <div class="card border-0 shadow-sm rounded-4 overflow-hidden">
    <div class="card-header bg-white border-bottom py-3 px-4">
      <div class="d-flex justify-content-between align-items-center">
        <form method="get" action="/admin/products" class="d-flex gap-2">
          <input type="hidden" name="deleted" value="<%= showDeleted ? '1' : '' %>" />
          <div class="input-group">
             <span class="input-group-text bg-light border-0"><i class="bi bi-search text-muted"></i></span>
             <input class="form-control bg-light border-0 shadow-none" name="q" value="<%= q || '' %>" placeholder="Tìm kiếm sản phẩm..." style="width: 250px;" />
             <button class="btn btn-secondary border-0 fw-medium" type="submit">Tìm</button>
          </div>
        </form>
        <div class="d-flex gap-2">
            <a class="btn btn-sm <%= !showDeleted ? 'btn-dark' : 'btn-light border' %> rounded-pill px-3 fw-medium" href="/admin/products">Đang bán</a>
            <a class="btn btn-sm <%= showDeleted ? 'btn-dark' : 'btn-light border' %> rounded-pill px-3 fw-medium" href="/admin/products?deleted=1">Đã ngừng bán</a>
        </div>
      </div>
    </div>

    <div class="card-body p-0">
      <% if (!rows || rows.length === 0) { %>
        <div class="text-center py-5 text-muted">
            <i class="bi bi-box fs-1 d-block mb-2 opacity-50"></i>
            Chưa có dữ liệu sản phẩm.
        </div>
      <% } else { %>
        <div class="table-responsive">
          <table class="table-modern">
            <thead>
              <tr>
                <th width="80">#</th>
                <th>Tên sản phẩm</th>
                <th>Danh mục</th>
                <th class="text-end">Giá (VNĐ)</th>
                <th class="text-end">Tồn kho</th>
                <th class="text-center">Tình trạng</th>
                <th class="text-end">Thao tác</th>
              </tr>
            </thead>
            <tbody>
              <% rows.forEach((r) => { %>
                <tr>
                  <td class="fw-semibold text-muted">#<%= r.id %></td>
                  <td class="fw-bold text-dark"><%= r.ten %></td>
                  <td><span class="badge bg-light text-secondary border"><%= r.theLoai ? r.theLoai.ten : 'Không có' %></span></td>
                  <td class="text-end fw-bold text-primary"><%= Number(r.gia || 0).toLocaleString('vi-VN') %></td>
                  <td class="text-end">
                    <% if (r.ton_kho > 0) { %>
                        <span class="fw-medium text-success"><%= r.ton_kho %></span>
                    <% } else { %>
                        <span class="text-danger fw-medium">Hết hàng</span>
                    <% } %>
                  </td>
                  <td class="text-center">
                    <% if (!r.deleted_at) { %>
                        <span class="badge bg-success border border-success px-2 py-1 rounded-pill">Đang bán</span>
                    <% } else { %>
                        <span class="badge bg-secondary border border-secondary px-2 py-1 rounded-pill">Đã ngừng</span>
                    <% } %>
                  </td>
                  <td class="text-end">
                    <div class="d-flex gap-2 justify-content-end">
                      <% if (!r.deleted_at) { %>
                        <a class="btn btn-sm btn-light border text-primary transition hover-lift" href="/admin/products/<%= r.id %>/edit" title="Sửa">
                          <i class="bi bi-pencil-square"></i>
                        </a>
                        <form method="post" action="/admin/products/<%= r.id %>" class="m-0">
                          <input type="hidden" name="_method" value="DELETE" />
                          <button class="btn btn-sm btn-light border text-danger transition hover-lift" type="submit" title="Ngừng bán">
                            <i class="bi bi-trash"></i>
                          </button>
                        </form>
                      <% } else { %>
                        <form method="post" action="/admin/products/<%= r.id %>/restore" class="m-0">
                          <input type="hidden" name="_method" value="PATCH" />
                          <button class="btn btn-sm btn-success rounded-pill px-3 fw-medium transition hover-lift text-white" type="submit">
                            <i class="bi bi-arrow-counterclockwise me-1"></i> Khôi phục
                          </button>
                        </form>
                      <% } %>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } %>
    </div>
    
    <div class="card-footer bg-white border-top py-3 px-4 d-flex justify-content-between align-items-center">
      <div class="text-muted small fw-medium">Trang <%= page %></div>
      <div class="d-flex gap-2">
        <% if (page > 1) { %>
          <a class="btn btn-sm btn-light border rounded-pill px-3 fw-medium" href="/admin/products?<%= new URLSearchParams({ deleted: showDeleted ? '1' : '', q: q || '', page: String(page - 1), pageSize: String(pageSize) }).toString() %>"><i class="bi bi-chevron-left"></i> Trước</a>
        <% } %>
        <% if (hasNext) { %>
          <a class="btn btn-sm btn-light border rounded-pill px-3 fw-medium" href="/admin/products?<%= new URLSearchParams({ deleted: showDeleted ? '1' : '', q: q || '', page: String(page + 1), pageSize: String(pageSize) }).toString() %>">Sau <i class="bi bi-chevron-right"></i></a>
        <% } %>
      </div>
    </div>
  </div>
</main>
<style>
  .hover-lift:hover { transform: translateY(-2px); }
  .transition { transition: all 0.2s ease; }
</style>

<%- include('../partials/foot') %>
