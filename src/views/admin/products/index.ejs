<%- include('../partials/head') %>
<%- include('../partials/nav') %>

<main class="container pb-5">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h4 mb-0">Sản phẩm</h1>
    <div class="d-flex gap-2">
      <form method="get" action="/admin/products" class="d-flex gap-2">
        <input type="hidden" name="deleted" value="<%= showDeleted ? '1' : '' %>" />
        <input class="form-control form-control-sm" name="q" value="<%= q || '' %>" placeholder="tìm kiếm..." />
        <button class="btn btn-sm btn-outline-secondary" type="submit">Tìm</button>
      </form>
      <% if (showDeleted) { %>
        <a class="btn btn-sm btn-outline-secondary" href="/admin/products">Đang hoạt động</a>
      <% } else { %>
        <a class="btn btn-sm btn-outline-secondary" href="/admin/products?deleted=1">Đã xóa</a>
      <% } %>
      <a class="btn btn-primary btn-sm" href="/admin/products/new">Thêm mới</a>
    </div>
  </div>

  <% if (!rows || rows.length === 0) { %>
    <div class="alert alert-secondary">Trống.</div>
  <% } else { %>
    <div class="table-responsive">
      <table class="table align-middle">
        <thead>
          <tr>
            <th>#</th>
            <th>Tên</th>
            <th>Danh mục</th>
            <th class="text-end">Giá</th>
            <th class="text-end">Tồn kho</th>
            <th>Đã xóa</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% rows.forEach((r) => { %>
            <tr>
              <td><%= r.id %></td>
              <td><%= r.ten %></td>
              <td><%= r.theLoai ? r.theLoai.ten : '' %></td>
              <td class="text-end"><%= Number(r.gia || 0).toLocaleString('vi-VN') %></td>
              <td class="text-end"><%= r.ton_kho %></td>
              <td><%= r.deleted_at ? 'có' : 'không' %></td>
              <td class="text-end">
                <% if (!r.deleted_at) { %>
                  <a class="btn btn-sm btn-outline-secondary" href="/admin/products/<%= r.id %>/edit">Sửa</a>
                  <form method="post" action="/admin/products/<%= r.id %>" class="d-inline">
                    <input type="hidden" name="_method" value="DELETE" />
                    <button class="btn btn-sm btn-outline-danger" type="submit">Xóa</button>
                  </form>
                <% } else { %>
                  <form method="post" action="/admin/products/<%= r.id %>/restore" class="d-inline">
                    <input type="hidden" name="_method" value="PATCH" />
                    <button class="btn btn-sm btn-outline-success" type="submit">Khôi phục</button>
                  </form>
                <% } %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% } %>

  <div class="d-flex justify-content-between align-items-center mt-3">
    <div class="text-muted small">Trang <%= page %></div>
    <div class="d-flex gap-2">
      <% if (page > 1) { %>
        <a class="btn btn-sm btn-outline-secondary" href="/admin/products?<%= new URLSearchParams({ deleted: showDeleted ? '1' : '', q: q || '', page: String(page - 1), pageSize: String(pageSize) }).toString() %>">Trước</a>
      <% } %>
      <% if (hasNext) { %>
        <a class="btn btn-sm btn-outline-secondary" href="/admin/products?<%= new URLSearchParams({ deleted: showDeleted ? '1' : '', q: q || '', page: String(page + 1), pageSize: String(pageSize) }).toString() %>">Sau</a>
      <% } %>
    </div>
  </div>
</main>

<%- include('../partials/foot') %>
