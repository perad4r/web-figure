<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/admin/dashboard">Quản trị</a>
    <div class="d-flex gap-2">
      <a class="btn btn-outline-light btn-sm" href="/san-pham">Trang bán</a>
      <form method="post" action="/dang-xuat" class="mb-0">
        <button class="btn btn-outline-light btn-sm" type="submit">Đăng xuất</button>
      </form>
    </div>
  </div>
</nav>

<div class="container py-3">
  <div class="d-flex gap-2 flex-wrap">
    <a class="btn btn-sm btn-outline-secondary" href="/admin/dashboard">Bảng điều khiển</a>
    <% if (user && user.role === 0) { %>
      <a class="btn btn-sm btn-outline-secondary" href="/admin/categories">Danh mục</a>
      <a class="btn btn-sm btn-outline-secondary" href="/admin/products">Sản phẩm</a>
      <a class="btn btn-sm btn-outline-secondary" href="/admin/variants">Biến thể</a>
      <a class="btn btn-sm btn-outline-secondary" href="/admin/colors">Màu sắc</a>
      <a class="btn btn-sm btn-outline-secondary" href="/admin/sizes">Kích cỡ</a>
    <% } %>
    <a class="btn btn-sm btn-outline-secondary" href="/admin/orders">Đơn hàng</a>
    <% if (user && user.role === 0) { %>
      <a class="btn btn-sm btn-outline-secondary" href="/admin/users">Người dùng</a>
    <% } %>
    <a class="btn btn-sm btn-outline-secondary" href="/admin/customers">Khách hàng</a>
    <a class="btn btn-sm btn-outline-secondary" href="/admin/reviews">Đánh giá</a>
  </div>
</div>

<% if (flash && flash.message) { %>
  <div class="container">
    <div class="alert alert-<%= flash.type || 'info' %>"><%= flash.message %></div>
  </div>
<% } %>
